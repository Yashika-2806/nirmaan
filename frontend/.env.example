# Frontend Configuration (safe template)
# Browser-facing API calls go through the local /api proxy route.
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_APP_URL=http://localhost:3500

# Server-side proxy target for /api routes
BACKEND_API_URL=http://localhost:8000/api

# Judge0 execution proxy target
JUDGE0_API_URL=https://ce.judge0.com

# Optional analytics tuning
NEXT_PUBLIC_ANALYTICS_ENABLED=true
